# Silence funding noise and only surface high/critical audit issues.
# Note: we intentionally do NOT set `deprecation` here — npm 10 marks
# it as "Unknown project config" and ignores it. The Dockerfile relies
# on the `npm ci --no-deprecation --no-fund` CLI flags instead, which
# are the supported way to silence those warnings.
fund=false
audit-level=high
